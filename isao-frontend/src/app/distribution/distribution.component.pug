.distribution
  .flex
    .left
      .entry
        .title You have
        .value <b>{{$isao.tokensOrderedByUser|async}}</b> Tokens
      .entry
        input(
          type="text",
          [(ngModel)]="str",
          id="byTokenInput",
          size="20", maxlength="20",
          placeholder="Enter amount in tokens")
        button(
          *ngIf="!$isao.process.reseivingTokens",
          kendoButton,
          id="byTokensBtn",
          (click)="reseiveTokens(str)") Receive tokens
        span.k-i-loading.k-icon(
          *ngIf="$isao.process.reseivingTokens",
          style="font-size: 24px; color: #ff6358")
    .right
      isao-timer
